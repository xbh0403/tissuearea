name: tissuearea
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      # the package itself (editable) + all Python deps from pyproject.toml,
      # including openslide-bin, which supplies the native OpenSlide library
      - -e .
